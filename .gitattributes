# 텍스트 파일은 저장소에 LF 로 저장하고 체크아웃 시 OS 에 맞춘다.
# 지정하지 않으면 각자 OS 기본값을 따라 줄바꿈만 바뀐 diff 가 생긴다.
* text=auto

/server/gradlew text eol=lf
/.githooks/* text eol=lf
*.sh text eol=lf
*.bat text eol=crlf
*.jar binary
