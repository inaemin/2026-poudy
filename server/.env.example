# 로컬 개발용 환경 변수 견본.
#
#   cp .env.example .env
#
# .env 는 커밋되지 않는다. 실제 비밀번호는 여기가 아니라 .env 에만 적는다.
# 실제 환경 변수가 .env 보다 우선하므로, CI 와 배포 환경 값이 항상 이긴다.
# 값이 하나라도 비면 앱은 커넥션을 맺기 전에 실패한다.

DB_URL=jdbc:postgresql://localhost:5432/poudy
DB_USERNAME=poudy
DB_PASSWORD=
